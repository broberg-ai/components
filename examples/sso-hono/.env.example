# Everything this app knows about identity. There is no other configuration,
# and no identity code in server.ts beyond mounting the package.
BID_ISSUER=https://id.broberg.ai
SSO_CLIENT_ID=sso-hono-example
SSO_REDIRECT_URI=http://localhost:8123/auth/callback
SSO_COOKIE_SECRET=change-me-openssl-rand-hex-32-at-least-32-chars
