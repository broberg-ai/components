# Moved → upmetrics-F019

This plan was moved to the **Upmetrics** project (`broberg-ai/upmetrics`) in a
cross-project move from `components-F027`. See `docs/features/F019-deployment-mgmt.md` there.

_This file is a tombstone left by cardmem so links don't 404 — the live plan is in upmetrics._