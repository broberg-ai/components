# F044.1 — @broberg/speech-dictionary edit API (components' half of the cardmem editor)

**Status:** proposed — pending cardmem's confirmation they'll build the UI half (F185-adjacent settings surface).
**Owner:** `components` (this endpoint; the git-commit + version-bump + tag-push + npm-publish mechanics belong to whoever owns the package, same as every other `@broberg/*`).
**Depends on:** F044 (`@broberg/speech-dictionary` v0.1.0, already built) and its Trusted Publisher being set up (same bootstrap-then-OIDC path as every other package this cycle).
**Requested by:** Christian, in two steps — first "let mig redigere + auto-publish ved gem" (no live-API alternative — ruled out, breaks Trail's offline on-device use case), then "jeg orker ikke flere auth sider og user logins," then "kan det redigeres i cardmem et sted, det er jo en del af vores fleet og SDLC."

## Motivation
Christian wants to add/fix dictionary entries without touching a terminal or waiting on a cc session, and without a new login screen. He's already authenticated in cardmem daily (it's the fleet's SDLC hub) — editing from there means zero *new* auth surface for him, even though the actual git/npm mechanics still need to happen somewhere. That "somewhere" has to be components (the package owner), not cardmem, so the two repos' release pipelines don't get tangled.

## Scope (in)
1. **A single authenticated write endpoint on Discovery** (components' one live service): `POST discovery.broberg.ai/api/speech-dictionary/edit`.
   - **Auth: trust-on-first-use, not a pre-shared secret** (revised 2026-07-03 — see incident below). Header `x-speech-dict-key` + body `session` field. The caller generates its OWN key locally (`openssl rand -hex 32`) and never hands the raw value to components or any other session — the first call for a given `session` name binds `sha256(key)` in a dedicated `speech_dict_edit_keys` table (same shape as F039's enroll `session_keys`, but its own table: an edit key can commit to git, an enroll key can only self-report a version — the two scopes must never share a binding). No secret ever needs to travel through a chat/intercom channel, because no secret is ever shared between two parties.
   - Body is a **diff, not a full-file replace** — `{ addTerms?: TermEntry[], removeTerms?: string[], addCorrections?: CorrectionEntry[], removeCorrections?: string[] }` — so concurrent edits don't clobber each other and commits stay readable (one clear diff per save, not a giant file rewrite).
2. **Mechanics on save:** read current `packages/speech-dictionary/data/{terms,corrections}.json` from `broberg-ai/components` via the GitHub Contents API, apply the diff, bump `packages/speech-dictionary/package.json`'s patch version, commit both files + the version bump, create + push a `speech-dictionary-v<version>` tag. No local git clone needed on the Discovery Fly machine — GitHub's Contents/Git-refs API does file+commit+tag over HTTPS.
3. **The existing tag-triggered OIDC publish workflow does the rest** (once Trusted Publisher is set up for this package) — no OTP, no login, same as every other `@broberg/*` release.
4. **Response** — `{ ok: true, version, tag, commitSha }` or `{ ok: false, error }`, so cardmem's UI can show a clear "published as v0.1.2" confirmation (or a real error) rather than a bare 200.

## Scope (out)
- **No UI.** That's cardmem's half (F185-adjacent or a new small fleet-settings surface — cardmem's call where it lives in their own information architecture).
- **No generic "edit any file in any repo" API.** This endpoint is narrowly scoped to `speech-dictionary`'s two data files + its own version — not a general components-repo-write backdoor. A future second editable package would get its own narrowly-scoped endpoint, not a parameterized any-file version (YAGNI until a second real case exists).
- **No optimistic-locking / conflict resolution beyond "last write wins."** Single editor (Christian) today; not worth building for concurrent-edit races that don't exist yet.

## Contract cardmem needs to build against
Refined per cardmem's #15417 review (grouped diff shape, dedup, rich response) — adopted as-is, their catch was correct.

```
POST https://discovery.broberg.ai/api/speech-dictionary/edit
Headers: x-speech-dict-key: <cardmem's OWN self-generated key — never sent to/by components>
Body: {
  "session": "cardmem",  // first call with a given key+session binds them (TOFU)
  "addTerms"?: { "products"?: string[], "people"?: string[], "brands"?: string[], "tech"?: string[] },
  "removeTerms"?: { "products"?: string[], "people"?: string[], "brands"?: string[], "tech"?: string[] },
  "addCorrections"?: [{ "wrong": string, "right": string, "note"?: string }],
  "removeCorrections"?: [string]  // by "wrong" value
}
Response 200: {
  "ok": true, "version": "0.1.2", "tag": "speech-dictionary-v0.1.2", "commitSha": "...",
  "added": { "terms": {...}, "corrections": [...] },     // what actually landed
  "removed": { "terms": {...}, "corrections": [...] }
}
Response 4xx/5xx: { "ok": false, "error": string }
```

- **Server-side dedup is authoritative**: adding a term already present in its group, or a correction whose `wrong` key already exists, is a no-op for that entry (not an error) — keeps the endpoint idempotent and protects the "terms/corrections can't drift" invariant from F044's AC.
- **Auto-publish confirmed**: yes — the tag push triggers the existing OIDC workflow (once Trusted Publisher is set up for this package, same one-time setup as every other `@broberg/*` package). No per-edit OTP, no login, ever — that's the entire point of the tag-triggered pipeline.
- **Version bump confirmed**: patch, always. A dictionary content edit is neither a breaking change nor a new feature — semver patch is the correct signal, and it's what every consumer's exact-pin re-bump would expect to see.

## Incident (2026-07-02): pre-shared secret leaked into intercom chat, redesigned to TOFU
The first cut of this endpoint used a single pre-shared `SPEECH_DICT_EDIT_KEY` that components generated and had to hand to cardmem. That handoff has no safe channel between two cc sessions — it was pasted directly into intercom messages (twice), which get captured verbatim into Trail's neuron index, i.e. a live secret landed in a searchable knowledge base. Direct violation of CLAUDE.md's "Secrets: openssl rand -hex 32, altid i .env, aldrig inline" — and a repeat of an earlier same-session mistake (Cloudflare Turnstile keys re-pasted in chat) that had already been called out once.

**Fix, not just rotation:** the leaked key was unset from Fly secrets immediately, but the real fix is architectural — removed the pre-shared secret entirely and switched to trust-on-first-use (same pattern F039's `/api/enroll` already uses for exactly this reason: "no central key to distribute, no human in the loop"). cardmem generates its own key locally and never sends it to components or through any channel components could echo it back through; the first authenticated call binds it. This isn't a mitigation for the leak, it's the removal of the entire class of mistake — there is no longer a secret that ever needs to travel between two parties.

## Open items before building
- **Christian's direct go + UI placement** — cardmem wants this from him directly, not relayed. Their own instinct: a small **Fleet** settings surface (the vocabulary is fleet-shared, not F185-specific), not embedded inside the interview module. Confirmed by Christian: Fleet surface, go given.
- Christian issues components a GitHub token with contents:write on `broberg-ai/components`, stored as a Discovery Fly secret (`GITHUB_WRITE_TOKEN`) — components does not have repo-write credentials on Discovery today (it only reads its own checked-in `inventory-data.mjs` at deploy time). Still pending.
